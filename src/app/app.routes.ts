import { Routes } from '@angular/router';
import { InscripcioComponent } from './components/inscripcio/inscripcio.component';

export const routes: Routes = [
  { path: '', component: InscripcioComponent },
];
